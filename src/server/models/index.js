const User = require('./user');
const UserAskedQuestion = require('./userAskedQuestion');
const MailRequest = require('./mailRequest');

module.exports = {
  User,
  UserAskedQuestion,
  MailRequest,
};
